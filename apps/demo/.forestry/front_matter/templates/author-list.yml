---
label: Author List
hide_body: true
fields:
  - name: authors
    type: list
    config:
      use_select: true
      min:
      max:
      source:
        type: pages
        section: authors
    label: Authors
