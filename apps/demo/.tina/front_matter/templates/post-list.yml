label: Post List
hide_body: true
fields:
  - name: posts
    type: field_group_list
    fields:
      - name: post
        type: select
        config:
          required: false
          options: []
          source:
            type: pages
            section: posts
        label: Post
    config:
      ? min
      ? max
      ? labelField
    label: Posts
