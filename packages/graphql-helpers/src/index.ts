import {
  arrayToObject,
  friendlyName as friendlyFMTName,
  friendlyName2 as friendlyName,
  templateTypeName,
  templateName,
  shortFMTName,
  slugify,
} from "./util";
import {
  queryBuilder,
  mutationGenerator,
  queryGenerator,
  formBuilder,
  queryToMutation,
  splitDataNode,
} from "./queryBuilder";
export {
  slugify,
  splitDataNode,
  queryToMutation,
  formBuilder,
  queryGenerator,
  friendlyFMTName,
  templateTypeName,
  mutationGenerator,
  templateName,
  friendlyName,
  shortFMTName,
  arrayToObject,
  queryBuilder,
};
